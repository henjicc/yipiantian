# 4.2 玩法稳定性与实际性能交接

状态：本机玩法稳定性、1080p持续运行和4K六组对照均已完成；主代理统一提交。生产基线 `d193129ff705e9a52480f789dbd065340c653d17`（包含退出清理修复），未用旧视觉中断结果补足时长。此结论不扩展为低配／跨系统承诺或最终用户主观试玩通过。

## 正式 1080p 结果

本机 Windows 10 19045、Ryzen 9 5900X、64GB、RTX4090 驱动610.47，Godot4.7.2／Forward+ Vulkan，标准4×MSAA、VSync开启、60fps上限。实际图像与窗口均1920×1080。用户原有旧prototype实例PID48360自03:06运行，按规则保留且未操作；其他代理停止渲染和编码，整卡遥测包含既有桌面负载，不能称为独占整机。

`.local/verification/4.2-visual-v2-1080p`：同一场景真实运行1849.495秒（30分49.5秒），外部进程1856秒，201项0失败、28个完整60秒样本，退出0、stderr空白。18个静态矩阵条件、两种镜头运动和八个连续尾段都保存逐帧CSV；全部前台样本0次失焦。六个标准静态条件约60.02fps：

| 标准画质条件 | p95帧间隔ms | p99帧间隔ms | 渲染CPU含setup均值ms | GPU均值ms |
|---|---:|---:|---:|---:|
| 白昼全景 | 17.009 | 17.286 | 0.704 | 3.427 |
| 白昼聚焦 | 17.034 | 17.468 | 0.733 | 2.944 |
| 白昼布置 | 17.121 | 17.526 | 0.736 | 2.821 |
| 夜间全景 | 17.024 | 17.334 | 0.718 | 2.791 |
| 夜间聚焦 | 17.030 | 17.335 | 0.734 | 2.850 |
| 夜间布置 | 17.002 | 17.326 | 0.714 | 3.377 |

- 全28组p95为16.977–17.121ms、p99为17.260–17.543ms，满足本机1080p的20／33.4ms门槛；实际最大单帧36.217ms也保留，不宣称没有偶发长帧。慢推进／转动各60秒，p95均约17.04ms，p99为17.35／17.37ms。
- 18组窗口所属线程CPU按累计耗时／drawn-frame差估计约2.11–2.50ms/帧；这是1Hz分段均值估计，非每帧CPU百分位。表内CPU只包括render+setup，排除脚本／物理；原始延迟process monitor另存，不能互相替代。
- 白昼实际绘制图元全景1,381,042→784,511，聚焦919,241→558,300。GPU频率在整段345–2685MHz间变化、P0/P3/P5/P8均出现，且60fps限帧；LOD减少几何已实证，不能据本轮差值宣称稳定比例的GPU加速。景深成本保留三组实测，不将虚化解释成几何减少。
- 峰值驻留工作集856,186,880B（约0.856GB，低于2GiB）；后台PCM采集前稳定约844.58MB，之后第24–30分钟约848.10–848.13MB，只有约3.5MB阶跃，未见本轮持续增长。私有提交峰值2,177,921,024B是另一内存口径，明确单列；不承诺多日运行无泄漏。
- 启动至场景可操作上界5.772秒，包含隔离夹具与首帧读回；普通发行实际菜单响应另测7.013秒。两者不混算，均非冷开机启动测试。
- 最小化30.0036秒实际14.9982fps，捕获1,440,256个PCM帧且峰值0，恢复峰值0.07986；UTC32.002秒与生长31.544秒的差0.458秒处于结算间隔内。恢复声音／焦点与后续自动保存正常。

原始证据：`hardware.json`、`ready.json`、`results.json`、各条件CSV／PNG、`windows-process.csv`、`windows-summary.json`、`gpu-telemetry.csv`、`background.json`。可直接阅读的归纳为`analysis-cases.csv`和`analysis-summary.json`，所有统计可回溯原值。

## 独立 4K 对照

`.local/verification/4.2-visual-v2-4k`：实际图像／原生窗口3840×2160，独立进程场景401.759秒，56项0失败、退出0、stderr空；六条件各60秒、预热5秒。峰值工作集953,847,808B，平均约60.01–60.02fps，0次前台丢失。没有声称这次约6.7分钟等同4K持续30分钟。

| 白昼4K条件 | p95 ms | p99 ms | GPU均值ms |
|---|---:|---:|---:|
| 全景全高 | 17.370 | 17.483 | 4.464 |
| 全景LOD | 17.085 | 17.434 | 4.091 |
| 全景LOD+景深 | 17.006 | 17.312 | 4.460 |
| 聚焦全高 | 17.045 | 17.322 | 4.396 |
| 聚焦LOD | 17.010 | 17.302 | 4.692 |
| 聚焦LOD+景深 | 17.023 | 17.305 | 5.500 |

渲染CPU含setup均值0.713–0.760ms，窗口线程CPU分段估计2.01–2.61ms/帧；图元降幅与1080p一致，LOD+景深聚焦相对仅LOD的GPU均值约增加0.808ms，但限帧和动态频率仍限制跨组因果结论。逐组CSV／PNG和`analysis-cases.csv`齐全，已查看最终4K近景，目标田／农事界面完整。

## 测量入口与边界

- `tests/performance_validation.gd`：正式主场景、真实 UTC／单调墙钟，隔离农场与设置，54 成熟株和三装饰；白昼／夜晚 × 全景／聚焦／布置 × 全高／LOD／LOD+景深。各条件预热5秒、实际采样60秒，另测缓慢推进／转动、真实最小化30秒；1080p进程连续至少1800秒。4K单独六条件。短 `smoke` 使用2秒样本、1秒预热、5秒后台，明确不满足验收时长。
- `tests/run-performance-validation.ps1 -Suite acceptance|4k|smoke -Directory <新的 .local/verification 子目录>`：标准 Godot 可执行程序运行正式场景，**不是发行包逐帧测量**。采集真实帧间隔、渲染CPU含setup、GPU、图元；Windows每秒工作集与窗口所属线程累计CPU另记，线程值不是每帧CPU百分位。NVIDIA时钟／功率是整卡指标，不改变驱动策略。
- `tests/release_startup_probe.ps1`：普通发行程序、进程局部APPDATA隔离，实际鼠标打开设置并截图；响应上界从游戏进程启动计时，截图需人工核对。发行日志有缓冲，不能作为即时就绪信号。
- `tests/start-isolated-game.ps1`、`native-game-window.ps1`、`native-game-lifecycle.ps1`：复用原生真实窗口与限定PID／启动时间工具；仅自有测试进程可暂停，不暂停系统或用户进程。最后一个助手已随4.1提交，窗口工具后续候选路径扩展归5.2。

## 已取得证据

- 纯状态：`.local/verification/4.2-pure`，农场78、存档42、装饰47、设置34项均0失败。
- 4.1共享普通发行证据：`.local/verification/4.1-release-native`，三路音量、质量／景深、窗口↔全屏、退出重开、DPI144、960×600钳制、原生作物弹窗通过。此为当时UI构建，不能冒称新视觉性能结果。
- 新视觉基线 `b266e7b`（纯文档HEAD `a3586bd`）完整短链：`.local/verification/4.2-visual-v2-smoke`，164项0失败、101.9秒、峰值工作集851435520B；18组加镜头／后台恢复均实际执行。后台约15fps，真实PCM静音，恢复有声，UTC继续。短链不算30分钟。
- 普通发行启动及暂停：`.local/verification/4.2-release-startup-v2-02`，真实设置响应上界7.013秒，工作集834326528B；只暂停PID65992约15.038秒，58线程全部恢复，前后生长与UTC均增加59.116秒（包含操作等待），无奖励增加。不是实际OS休眠。
- 实际只读保存失败：`.local/verification/4.2-release-faults`，内存收获1篮、磁盘0篮、关闭留窗；解除主档只读后重试仅保存1篮，田03为空，“已保存”截图与最终档一致，退出0。`readonly-verification.json`与03／04／05截图可追溯；所有副本位于局部APPDATA。

## 失败修复与补充真实窗口验证

- 旧视觉 `.local/verification/4.2-1080p-final` 约380.4秒被根代理主动中断以修正画面，`interruption.json`明确未完成30分钟，不得混入新基线正式结果。首次smoke的typed-array错误、首轮启动探针等待缓冲日志错误均是已修夹具问题，失败目录保留。
- 新视觉旧包两次短时普通退出出现12个ObjectDB／6个资源残留，exit0不等于干净退出；无暂停的PID68256也出现，不能归因于进程暂停。后续旧包verbose未再次复现，不称为稳定必现；历史verbose与引擎清理契约支持声音生命周期根因判断。声音代理统一正常／未加载／明确放弃退出并加入幂等`FarmAudio.shutdown()`，在`d193129`提交；45项隔离回归和三路实际普通包退出均无残留。细节见[退出补修交接](4.2-exit-cleanup-handoff.md)。性能夹具仅在测量完成后复用该方法，正式长测收尾stderr也为空。
- 新包较新版拒绝复用`.local/verification/exit-cleanup/after-unloaded`：真实窗口显示拒绝，原始`{"version":999}`与退出后的SHA均为`8EEDF8924C22CFE64F5530CE2DAA51F4BD552C93327B3EDB9636B32DDF82C1E7`，不创建备份或pending；只测首次拒绝与正常关闭，没有声称再次点击重试。
- 新包坏主档显式恢复：`.local/verification/4.2-release-corrupt`，PID57204实际显示恢复提示，鼠标点击后青菜1篮／田03空；唯一`farm.unreadable.*`归档SHA与原始截断文件完全相等，退出0、stderr空。`verification-summary.json`及01／02截图记录完整路径，未静默初始化。
- 普通验证包EXE SHA256 `B25DB65BFD1B39CCBA4788D8675EB9BAA53BA29C2ED485DE31D5739058FA6D43`；修复后PCK `CB19BCA4AE8C0820620C7E1B2A12B1FBE1CD7E62EA2A4723426672C8AEB8644E`。启动／暂停／只读首次证据的旧PCK为`86D64555…170C1`，其成功交互证据可保留，干净退出由新包补修回归与最终场景验证，不偷换构建身份。

## 视觉和验证边界

- 已实际查看最终1080p白昼全景／近景和夜间全景，与固定`参考_主界面.jpg`对照。后屋／六田／左门藤架／右桥船、纸色HUD和夜间可读性可核验；水岸、院落植被与远景层次较修正前增加。
- 相对参考，成熟作物密度、屋前生活物件、石岸和建筑的手绘笔触仍简化，水面反射和自然细节也不同；不能写完全还原。未为达到帧率临时减少植被／关闭水材质或灯光。最终审美接受仍由5.3复核，不把本机性能通过当成用户试玩或视觉完全一致。
- 实测设备仅本机Windows10 19045、Ryzen5900X、64GB、RTX4090／610.47；未验证低配、其他操作系统、实际系统睡眠、人的主观听感／长期试玩，不做相应承诺。

## 交付归属

本任务新增三项测试入口、任务记录与本交接；退出声音缺陷由3.4／4.3代理在主代理协调下定点修复并先行提交。普通生产游戏没有添加性能测试参数或修改生长速度。所有原始数据在`.local/verification`，不是交给玩家的发行内容。5.2仍需独立构建候选并验证仓库外运行，不能把普通验证目录直接称为最终包。

最终冻结路径：`tests/performance_validation.gd`、`tests/run-performance-validation.ps1`、`tests/release_startup_probe.ps1`、本交接、任务`4.2-验证完整玩法稳定性与实际性能.md`。没有改共享总览／开发说明／project-context，交由主代理同步。两次正式测试已实际执行成功，PowerShell解析与本任务diff空白检查通过；无理由再重复运行。

自有Godot PID73724／34192和NVIDIA采集子进程均已退出，GPU时隙已归还；最终CIM只见保留的用户既有Farm PID48360，不得因本任务完成将它关闭。

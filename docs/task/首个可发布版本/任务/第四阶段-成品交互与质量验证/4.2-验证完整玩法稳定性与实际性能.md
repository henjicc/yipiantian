# 4.2 验证完整玩法稳定性与实际性能

## 任务状态

- 状态：已完成本机约定验收；退出缺陷修复后，1080p真实30分49.5秒持续运行与4K六组对照通过。明确保留视觉差异、未测低配和实际OS睡眠边界；不代表最终用户主观试玩通过。
- 所属阶段：第四阶段——成品交互与质量验证。
- 前置任务：4.1。
- 关联任务：4.3、5.2、5.3。
- 是否可以并行：可与 4.3 整理素材、5.1 核对来源并行；性能测量时不同时录制、生成模型或压测其他任务。

## 任务目标

证明完整游戏在承诺环境下可持续正常使用，而不是仅在开发机打开一次或用离线视频展示流畅；修复阻断、丢档、误操作与有证据的性能问题。

## 当前情况

- 规则、存档、真实农事输入、布置、六阶段、氛围、景深和成品界面已接入；旧场景测试的设置隔离与节点路径适配由4.1完成，退出补修后完成本次正式测量。
- 开发机为 RTX 4090、64 GB、双 4K；旧约 60 帧主循环样本不能代替完整场景／最低设备验证。
- 旧实时采集出现重复帧，固定步长视频不是性能证据。

## 实施方案

按 1.1 已定硬件、系统、分辨率、画质与帧时间口径测量。分开规则正确性、真实输入、存档恢复、实际渲染与跨设备；不为形式搭建持续集成或更换测试框架。修复只针对复现问题，留真实结果和局限。

## 执行步骤

1. 汇总现有正式测试入口并运行相关全套，核对失败日志与退出码；在隔离存档中覆盖首次、成熟临界操作、重复收获、全部解锁、布置取消和重开。
2. 独立程序验证最小化／恢复、失焦／拖动释放、休眠回访、窗口／全屏、高 DPI 和正常退出；保存失败和坏档使用测试副本，不碰玩家档。按实施方案校对表对照 [主界面参考](../../参考_主界面.jpg)，记录默认全景及聚焦／转动中的视觉缺口，不能只验操作可用。
3. 在约定设备与最终最大代表场景记录帧时间分布、平均帧率、CPU／GPU 耗时、内存、启动／加载耗时；分别测全景、缓慢推进、转动、成熟满田、夜景和反馈效果。
4. 进行足够覆盖真实回访与资源增长的持续运行，检查内存是否持续增长、后台资源与声音是否符合策略；明确记录时长，不能将短测说成长期稳定。
5. 性能不达标先定位资源、材质、阴影或代码热点，再做必要实例化／预算调整；每次修复复测受影响场景，不无依据降画质或换引擎。
6. 缺目标设备时标明待验证并安排实际机器测试，或由用户明确收窄支持声明；不模拟低配后声称真实设备通过。
7. 将结果、阻断问题和回归入口写入任务记录及必要环境文档，交接给 5.2；有功能／资产变化后只复核受影响结论。
8. 在固定设备、镜头、分辨率和农场状态下分别测全高细节、仅 LOD、LOD 加景深；记录实际绘制图元、帧时间和画面差异。覆盖全景、聚焦田、同屏邻田、近处非焦点轮廓及 4K，检查慢推／缩放临界点／快速换田的跳变与首次加载卡顿。景深关闭后再次检查点选和清晰度，不能把虚化当作减少几何或性能已通过的证明。

## 涉及内容

### 新增文件

- `tests/`：真实缺陷对应的必要回归；隔离数据和原始测量放 `.local/`。

### 修改文件

- 仅复现缺陷对应的游戏模块、资源或导入配置；执行时逐项记录确切文件和原因。
- `docs/development-setup.md`：正式检查入口与已验证设备；本任务记录保存摘要和未覆盖边界。

## 验收标准

- [x] 规则、输入、存档、解锁、布置、设置和恢复的相关测试通过，已发现的严重阻断／崩溃／丢档问题清零。
- [x] 目标机器、分辨率、画质、测量场景和帧时间满足 1.1 口径，未验证环境不被写成支持承诺。
- [x] 真实缓慢推进／转动及持续运行有检查证据，离线视频未被用作性能通过依据。
- [x] 主界面视觉校对有真实截图和差异记录，环境丰富度、笔触及界面没有为达帧率而无记录降级。
- [x] 全景与非焦点区域的降面有实际证据，目标田细节和交互完整；景深成本单独可辨，三种观察状态及切换满足性能口径。
- [x] 失败修复有复现和回归，已知非阻断问题有记录与影响说明。
- [x] 原始数据可追溯、摘要可跨工具读取；本交接已交主代理用于同步总览与后续打包。

## 执行记录

### 2026-09-17

- 完成内容：准备真实墙钟采样与 Windows 外部采集，不占用前置任务的 GPU／导入时隙。
- 实际改动：新增 `tests/performance_validation.gd`、`tests/run-performance-validation.ps1`、`tests/release_startup_probe.ps1`；限定隔离发行 PID／启动时间的 `tests/native-game-lifecycle.ps1` 先随4.1共享验收提交。同一正式场景预置 54 成熟株与三件装饰，同时注入隔离 farm／preferences；普通发行包不增加测量入口。
- 验证结果：已通过 GDScript headless check-only 与 PowerShell 语法解析。独立纯规则78／农场存档42／装饰47／设置34项全部0失败，日志 `.local/verification/4.2-pure`；尚未运行最终实景采样，不能认作本任务整体验收。
- 待执行覆盖：既有纯规则／存档／装饰／设置、农事／布置／存档恢复／菜单／镜头／氛围集成回归；正常 release 真实输入、坏档与保存失败、焦点／最小化／全屏／高 DPI／测试进程暂停恢复。进程暂停与真实操作系统休眠明确区分，禁止改系统时钟或暂停用户整机。
- 采样计划：1080p 单进程实际运行至少30分钟，白昼／夜晚×全景／聚焦／布置×全高／仅LOD／LOD＋景深各预热5秒后采样60秒，再测缓慢推进／转动、后台实际静音和UTC生长；4K三组对照单独进程。短 smoke 只检查采集链，不计正式采样。
- 指标边界：每帧 CSV 保存真实间隔、CPU render／setup、GPU和绘制图元；Windows 每秒记录实际驻留工作集、进程与窗口所属线程累计CPU；NVIDIA现有工具可用时同时记时钟／功率，不改变驱动电源策略。Godot直接运行正式场景不冒称release逐帧，原样release另验证启动与工作集；不引入PresentMon等新依赖。
- 普通发行辅助验收：4.1 修正最小窗口后的 release 三次启动／退出均0，stderr空；实际鼠标设音量49%／39%／60%、低画质及景深偏好，保存全屏重开，再切窗口保存重开均保留。Win32实测DPI144（150%）、4K屏，普通全屏客户区3840×2162（Godot边界多2像素）；中文控件完整可点，原生最小化恢复与960×600最小尺寸钳制通过。原始截图、两版设置、三份农场和构建哈希见 `.local/verification/4.1-release-native/verification-summary.json`；增长176.883秒匹配实际UTC，六田与累计未重置。另以原生鼠标验证农事作物弹窗持续显示、白萝卜选择与Esc先关闭弹窗／先取消工具，截图16／18／19，第四进程退出0；修正依据为测试夹具PopupWindow路由而非产品失败。此轮与4.1共享证据，不重复冒称性能验收。
- 遗留问题：3.5 已交接、4.1 最终回归收尾；正式矩阵与持续运行仍未执行。只有本机高配设备，不承诺最低配置和未测系统。
- 与原计划的偏差：无。
- 对后续任务的影响：5.2 只能基于本任务通过的内容构建候选。
- 相关提交：准备基线 `62fc1a2`；后续实际验收基线按最终3.5／4.1提交记录，主代理统一提交本任务。

### 视觉修正前的中断记录

- 4.1 生产基线为 `4b3bf44`。首次短采集因夹具条件表达式丢失 typed Array 类型而停止，修正为显式 `assign`；`.local/verification/4.2-smoke` 保留失败记录，非产品故障。第二次 `.local/verification/4.2-smoke-02` 26 项0失败、退出0、stderr空，确认实际1080p、CPU／GPU／Windows线程与内存／驱动频率采集链可用。
- `.local/verification/4.2-1080p-final` 实际运行约380.4秒后，独立视觉复核确认整体画面未达到固定参考目标。依根代理要求，通过已核验PID38360的正常窗口关闭退出；采集器按缺少最终结果报未完成，所有自有Godot与NVIDIA采集进程已结束。`interruption.json` 明确 `completed_thirty_minutes=false`、`official_performance_pass=false`。此目录仅为视觉修正前的部分数据，不能算30分钟稳定性或正式性能通过，当前不执行4K矩阵。
- 实际视觉差距：近水大面积统一青灰、反射与水岸细节少；院落植被和小物件密度明显较低；建筑／土壤／叶片笔触及远山层次更简化。核心布局与HUD对应结构不能替代整体视觉验收。等待有界视觉修正后的生产基线，在新目录重新执行完整矩阵与30分钟。
- 下一轮普通release故障计划：使用已准备的 `.local/verification/4.2-release-faults` 隔离副本；保留有效主档和备份，依次测真实只读主档导致保存失败、原生重试不重发奖励、损坏主档保留字节且由可见恢复入口恢复、未来版本拒绝覆盖。另通过 `release_startup_probe.ps1` 观察实际菜单响应时间，再仅暂停已核验隔离进程15秒并恢复，按保存时间和生长差核对UTC连续性。暂停进程不冒称操作系统休眠；不修改系统时钟、不触碰玩家档。上述故障动作尚未执行。
- 补强短链路：新 smoke 与正式测量共用白昼／夜晚×全景／聚焦／布置×三组模式的18条件、缓慢推进／转动，以及真实最小化／静音／恢复。仅缩短为每组2秒、预热1秒、后台5秒，始终标记 `qualified_duration=false`；正式参数保持60秒／5秒／30秒和实际1800秒连续运行。GDScript与PowerShell解析通过；等待视觉冻结后首次运行该完整smoke，不把此前只含日间两组的26项结果当作新链路已执行。

### 视觉修正后与退出缺陷记录

- 视觉生产基线 `b266e7baf90f442cf3c88ace6a28554c253a04dc`，随后纯文档提交 `a3586bd` 不改变生产内容。水岸、植被和院落层次已做有界修正；不能据此宣称固定参考完全还原或用户主观试玩通过。
- 完整短链 `.local/verification/4.2-visual-v2-smoke`：164项0失败、101.9秒、退出0、stderr空，18条件及推进／转动／后台恢复均实际执行；最大工作集851435520字节。后台5.067秒实际14.999fps，捕获243200个PCM帧峰值0，恢复峰值0.08738；生长6.602秒与UTC7.043秒之差在结算间隔内。此为采集链验证，`qualified_duration=false`，不计60秒性能样本或30分钟稳定性。
- 普通发行启动 `.local/verification/4.2-release-startup-v2-02`：真实设置按钮点击后截图确认界面已响应，进程年龄上界7.013秒；外层PowerShell启动开销不混入游戏计时。第一次探针 `.local/verification/4.2-release-startup-v2` 误等发行日志实时刷新，因日志缓冲超时；保留失败记录，修正为初始隔离档存在后实际点击并人工核对截图，未修改产品启动逻辑。
- 同一普通程序真实播种后，仅暂停核验的PID65992共15.038秒，58个自有挂起线程全部恢复；前后存档UTC与生长增量均59.116秒（含操作等待），奖励仍0。不修改系统时间、不让整机休眠，真实操作系统休眠仍未覆盖。
- 普通发行真实只读故障 `.local/verification/4.2-release-faults/readonly-verification.json`：PID12000在主档只读时真实收获，内存田03清空／青菜1篮而磁盘仍0；窗口关闭被阻止，显示未保存与重试／仍然退出选项。解除精确隔离文件只读后重试，磁盘田03空、青菜恰好1篮；截图05确认“已保存”，随后正常退出0。预期`replace_main error=1`两次对应动作保存与关闭保存，未误发奖励、未触碰玩家档。
- 阻断正式长测：两次短时普通发行退出（PID68256无暂停、PID65992含暂停）均exit0但stderr报`12 ObjectDB instances were leaked`与`6 resources still in use`，因此不能当作干净退出。长开只读故障窗口未出现该残留，显示与声音播放／退出时序有关；主代理已安排声音所有者调查并修复，原始stderr均保留。当前GPU已交给其定点回归，等待新冻结基线后再执行正式矩阵与持续运行。

### 最终验收结果

- 退出补修已提交`d193129ff705e9a52480f789dbd065340c653d17`，统一允许退出后的声音清理；45项回归和三路真实普通包退出均无残留。旧包后续verbose未重现，不写稳定必现，依据和范围见[退出补修交接](../../handoffs/4.2-exit-cleanup-handoff.md)。新版PCK为`CB19BCA4AE8C0820620C7E1B2A12B1FBE1CD7E62EA2A4723426672C8AEB8644E`。
- 普通包坏主档实际点击恢复：损坏原字节归档、青菜1篮／田03空保持、exit0／stderr空；新版较新版本拒绝覆盖复用声音代理真实证据，前后SHA一致且无backup／pending。各入口均隔离玩家档。
- 正式1080p：`.local/verification/4.2-visual-v2-1080p`，同一场景真实1849.495秒，201项0失败、28个完整60秒样本，退出0且stderr空。标准6条件p95最高17.121ms、p99最高17.526ms；全28组p99最高17.543ms，实际最长单帧36.217ms仍如实保留。
- 内存／启动：驻留工作集峰856,186,880B，低于2GiB目标；后台采集前约844.58MB，后段约848.10–848.13MB，无本轮持续增长。私有提交峰2,177,921,024B与驻留口径不同，交接单列。正式场景启动5.772秒，普通包菜单响应7.013秒，均为上界且非冷开机测试。
- 真实最小化30.0036秒：14.9982fps、1,440,256 PCM帧峰值0，恢复峰值0.07986，UTC与生长差处于结算间隔内。自有进程15秒暂停另外验证，不冒称实际OS睡眠。
- 独立4K：`.local/verification/4.2-visual-v2-4k`，实际3840×2160，六条件各60秒，56项0失败、退出0且stderr空；p95最高17.370ms、p99最高17.483ms，峰值驻留953,847,808B。此约6.7分钟对照不冒称4K30分钟。
- 设备及负载：本机Win10 19045／Ryzen5900X／64GB／RTX4090驱动610.47；60fps限帧、VSync开启、标准4×MSAA。用户既有Farm PID48360始终保留，采样元数据明确其启动时间与用途，全卡指标含背景负载。未测低配／其他系统，不作相应承诺。
- 视觉边界：已对照固定参考与最终全景／近景／夜景／4K图。作物密度、屋前物件、石岸和手绘笔触仍较简化，水面细节不同；记录实际改善与差异，不称完全还原、不把性能通过当最终审美接受。未因指标临时削减正式资源。
- 完整指标、CPU／GPU口径、几何对照、原始CSV路径、故障证据和后续入口见[4.2交接](../../handoffs/4.2-handoff.md)。已向主代理报告并释放GPU；自有测试和采集进程全部结束，仅保留用户原有实例。普通包的最终仓库外候选验证继续交5.2，主代理同步开发说明和总览并提交。

# 3.4 完成昼夜氛围与游戏声音

## 任务状态

- 状态：实现与技术／视觉验收完成，待主代理提交（主观听感受工具能力限制，明确保留未验证）。
- 所属阶段：第三阶段——正式资产与完整玩法。
- 前置任务：2.3、3.2。
- 关联任务：3.3、4.1、4.2、4.3、5.1。
- 是否可以并行：可与 3.3 并行制作独立氛围和音频资源；主场景整合顺序需协调，避免同时改同一文件。

## 任务目标

形成温和、可读的昼夜变化和有完成感的操作声音；控制背景运行的打扰，保持现实时间生长独立于视觉和音频。

## 当前情况

- `main.tscn` 只有固定方向光、雾和环境；场地水岸为简化体块，没有随时间变化的系统。
- 当前无游戏声音，录制入口明确不录音；本任务加入声音不代表旧录制已经支持音轨。
- 玩法规则区分本地昼夜表现与 UTC 生长，首版不做实际太阳遮挡影响产量。

## 实施方案

Godot 负责光色、可见水面与轻微环境动态，不生成复杂天文／天气系统。音频采用明确可使用的来源或获准的生成方式，保留原始来源与处理记录；少量环境、播种／浇水／收获／界面声优先，背景音乐遵循 1.1 定稿。音量状态接口交给 4.1 接设置。

## 执行步骤

1. 以 [主界面参考](../../参考_主界面.jpg) 校准日间暖光、灰绿淡彩、水面和朦胧远景，再定义晨、昼、暮、夜的有限关键光色与平滑过渡，按本地时间取样；调试使用指定测试时间，不改变系统时钟。
2. 接入正式环境与灯笼的光照，检查夜间田况、工具和装饰可辨；纹理强阴影或高光问题回源修正。
3. 实现克制的水面和必要反馈效果，避免把参考图的静态笔触变成漂移噪声；暂停渲染或降低帧率不能改变生长结果。
4. 确定声音清单并核对来源，整理音量、循环接缝与格式；不给每个微动作叠多层高音量反馈。
5. 接入游戏事件和独立音量通道，重复点击／快速连续动作限制不必要叠音；按定稿处理失焦、最小化和恢复。
6. 在亮暗场景、全景与近景实测声音和画面，确认音乐／音效可静音并暴露设置接口；向 4.3 交接需要录制的声音通道。

## 涉及内容

### 新增文件

- `Game/art/`：采用的音频与必要氛围资源；`Game/`：昼夜、音频控制与必要效果。
- `ArtSource/`：采用音频／纹理的来源和可维护输入；具体位置实施时登记。

### 修改文件

- `Game/scenes/main.tscn`：最终集成灯光、环境与声音；独立资源确认后再合入。
- `docs/design-baseline/asset-list.md` 与开发准备：补声音资源和验证入口。

## 验收标准

- [x] 四种代表时段与过渡下均能辨认田况和操作；没有引入日照产量、天气或联网服务。
- [x] 昼夜变化／后台渲染不影响生长与离线结算结果。
- [x] 操作声技术上零削波、限制叠音，循环端点与真实跨末尾检查通过，音量／静音和失焦策略有效；主观接缝听感未验证。
- [x] 音频与新增素材来源可交给 5.1 复核，界面和录制任务知道音轨边界。
- [x] 画面与真实混音输出检查完成，采用资源、测试入口已交接，总览由主代理同步；工具不支持模型听音，未声称完成主观音色／节奏复核。

## 执行记录

### 2026-09-17

- 完成内容：新增原创程序合成的完整 128 秒配乐、64 秒日／夜环境与播种／浇水／收获／界面四个短音，提供 PCM24 母带、乐谱事件、合成与检测源、正式 OGG／WAV。音频分组、音量、限频、静音／恢复，本地时钟昼夜、灯笼光、水面、前后台帧率模块已实现，并由3.3接入完整院落后完成真实联调。
- 实际改动：`ArtSource/Audio/`、`Game/art/audio/`、`Game/audio/`、`Game/atmosphere/`、`Game/scenes/atmosphere_sample/`、`tests/atmosphere_test.gd`、`tests/atmosphere_scene_test.gd`、本任务及 [3.4 交接](../../handoffs/3.4-handoff.md)。主场景接入由 3.3 所有者进行，未抢改共享文件。
- 验证结果：独立 Godot 4.7.2 实窗最终 **41项全通过**，完整主场景 **23项全通过**；真实收获收益10→11且PCM非零，实际水shader、仅已放置灯笼亮、背景昼夜一致、原生最小化／恢复、后台≤15fps且UTC仍结算120秒。音量0、快速连击、跨循环与锚点连续检查通过。离线声音全部零削波、真峰值≤-13.07dBFS、循环端点无异常幅差。正式四时段全景与昼夜近景六图均已实看，主代理复看确认核心视觉通过；窗口均正常退出。
- 音轨验证：独立4.166667秒MovieMaker AVI确有48k双声道PCM，峰值.17160、零削波；转为NVENC H.264＋AAC后音画时长差<1ms。旧视频仍无音轨；3.3已获授权为record.ps1新增-WithAudio及限定开发录制的焦点策略，完整40秒有声片由其继续验证，本任务不冒称已验收该成片。
- 遗留边界：顶部裸深色HUD文字在夜景／聚焦物体上对比不足已交3.3补最小纸底、4.1统一整合，主代理确认不阻塞3.4核心交付。当前工具不支持模型直接听音，不能完成主观音色、节奏和循环听感复核，明确保留限制；不因此等待用户或安装新服务。
- 与原计划的偏差：媒体目录尝试因缺HeyGen CLI不可用，采用完整可复现的原创程序作曲和合成，不冒称真实乐器或田野录音。用户要求商务核验暂缓，未调查账户／订单／商业授权。
- 对后续任务的影响：3.3已获可用模块接口，4.1接三音量与统一设置持久化，4.3需显式保留和核验录制音轨。
- 相关提交：本代理未提交；等待完整任务完成后由主代理精确提交。

---
template_state: ready
---

# 项目上下文

> `template_state` 仅取 `pending` 或 `ready`，由初始化流程维护，不在其他文件复制状态。

## 当前事实

- 2026-09-17最新质感修订由主代理独立执行，用户明确禁止后续使用子代理。荷叶PCSS黑点改用柔边PCF解决；近岸倒影只混合反射色，不再按屏幕命中改变水面权重。新增草土混合土畦、短草、根部贴花和圆润河石，减弱景深；调整船泊位、吃水与随船水面遮罩。西侧入口棚移除，长竹架旋转90°沿栏杆、下方预留连续土床，灯笼挂点移到院内侧；攀爬作物玩法尚未接入，六田96格/v3不变。相关原理与本地排错证据见[开发准备](../docs/development-setup.md)，资产见[环境源说明](../ArtSource/Environment/README.md)。
- 项目“我有一片田”使用 `Game/` 正式Godot工程，主场景为 `scenes/main.tscn`。用户于2026-09-17明确推翻整田种植：六块大田各分4×4小格，同田允许青菜／白萝卜混种，逐格播种、浇水、收获；每格独立现实UTC生长，成熟单格收获1篮，原两作物时长与浇水比例不变。`Game/farm/` 持有96格权威状态及v3存档，v1／v2每旧田唯一作物迁到cell_06，其余为空，保留累计和装饰，升级写入前保留不可变旧原件。纯状态237项、存档289项、装饰47项通过；此前rc.3低视角、近岸前景、光影与逐格输入已完成；该版本功能／实景回归、1080p烟测164项与4K六组56项均通过，源码8f93348的rc.3独立干净构建、仓库外普通程序混种／逐格收获／保存重开及v2实际迁移均通过，018号56秒演示已留档。rc.3交付证据见[逐格整合交接](../docs/task/格子农田与画面重构/handoffs/整体验证-handoff.md)。接口和实际证据见[逐格状态与迁移交接](../docs/task/格子农田与画面重构/handoffs/种植状态与迁移-handoff.md)。六阶段正式资源、院落、原创声音、中文界面及独立偏好已接入；不增桌面宿主或联网。无CI。美术源放 `ArtSource/`、正式资源放 `Game/art/`，目录边界见[README](../README.md)。旧Unity工程按授权删除，软件未卸载。
- 历史候选rc.1（源码 `6b460f30`）与rc.2（源码 `a3163dcc`）均为整田玩法／v2存档，已各自完成独立构建与仓库外普通程序验证。rc.1的54株30分钟201项及4K56项、rc.2的96个视觉实例1080p烟测164项及4K56项，只属于各自旧基线，不能用来证明当前96格逐格状态或新画面性能。旧候选、截图、录屏保留作为历史；氛围版本详情见[整合交接](../docs/task/氛围提升/handoffs/整体验证-handoff.md)。
- 当前 Git 分支为 `main`，已配置仓库级 Git LFS 与文本属性；远端和上游尚未指定，不能声称已完成异机同步。源文件与配置纳入本地基线；缓存、构建、日志、`docs/ref/` 原始参考资料与 `.local/` 试验不入库。实际提交状态以 Git 为准。
- 开发关键节点已有按需录制入口 `scripts/record.ps1`，以真实 3840×2160 全屏和 NVENC 输出 H.264 / 60 CFR；自动展示用 Godot 固定步长逐帧输出，最终界面16秒实时探针仍检出约24%近重复，只作诊断；最新018号56秒有声离线素材对应逐格与视觉源码8f93348，3360帧、三段运动0近重复、音画同为56秒，录制开始时工作区干净；017对应旧氛围a3163dcc，016对应旧视觉b266e7b，015为更早历史，均保留。自动演示使用停留、缓慢聚焦、小角度转动与收尾，普通运行不启用；视频和中文剪辑索引只存 `制作留档/05_开发录屏/`。用法见开发准备，节奏与触发约定见测试规则。完整演示加入运动区间近重复帧检查；离线演示可用 `-WithAudio` 带游戏音轨，不采集麦克风或其他程序。离线演示与实时性能验收分开。
- 开发环境为 Windows / PowerShell；Godot 标准版及 Windows x86_64 导出模板已配置；Blender 官方 MCP 已在独立 AI 工作区通过保存、导出、重启和截图测试，保留普通 Blender 配置；Tripo 插件技能复用 CLI 授权。正式作物与院落高／低资源均有整理、导入和实景校对记录；当前成熟青菜2988／1494、补齐屋脊端面后的民居20985／6523三角形，其余数字见资产清单。资源三角形数不是实际绘制量或整机性能结论。实际版本、双屏布局、调用入口和验证边界见 [开发准备](../docs/development-setup.md)，原代表样片见 [1.2交接](../docs/task/首个可发布版本/handoffs/1.2-handoff.md)。

## 已确认基线与创意方向

- 用户于 2026-09-17 确认直接切换 Godot，不再做引擎对比实验。仍先制作 Windows 普通窗口版三维农场小游戏，正式桌面壁纸宿主后置。实现默认采用带类型标注的 GDScript，当前为 Forward+ / Vulkan；精确版本以 [.godot-version](../.godot-version) 为准，首个可发布版本计划已获用户授权自主实施；用户最新明确禁止后续使用子代理，改由主代理独立执行；进度与定稿边界见 [任务总览](../docs/task/首个可发布版本/00-任务总览.md)，尚未实现部分以各任务状态为准。
- 创意目标：与现实时间同步、可以照料和布置、会持续生长的微缩农场，强调轻松、低打扰的观察和操作体验。规则以此指导后续取舍，不把创意清单全部作为已批准功能。
- 最新视觉方向为架空中国古代田园：宋明江南民居与生活气质。用户已选定第二轮 01 图的观察距离、整体构图与感觉，主图保存在 [制作基准包](../docs/design-baseline/README.md)。随后明确要求更圆润简洁的卡通形体，反对摄影级蔬菜细节，现已确认 A 工笔淡彩全景与 A 淡彩青菜的风格和质感；固定参考、原始提示词与复用模板见 [风格基准 v1](../docs/design-baseline/style-guide.md)。B / C 等保留为历史探索，不再待选。主图负责构图和空间氛围，不再用其微小写实纹理否定后续风格修正。低多边形只是可选手段，不承诺严格朝代考据。
- 用户已明确希望较大农场的全景浏览，田块在全景中相对较小；点击某块田后相机平滑移动聚焦，再进行操作。允许围绕关注区域微调角度、平移和缩放；默认镜头应体现立面与空间层次，不能继续固定为上一轮近距离田格俯视。当前六田每格按权威状态显示正式三阶段作物；操作为全景点大田聚焦、镜头到达后选小格、再点工具按钮立即执行，不再使用整田工具或世界点击自动种植。Esc／右键先清格选择再返回；模型显示不承担格子身份，格子由稳定行列ID与土面位置决定。首版六田数量保留，旧候选技术验收仅覆盖旧版；当前格子农田与画面重构须独立验收，候选提交尚未冻结。用户明确允许只服务构图的镜头周边植物；风动、湖波、船与云层共同营造生活气息，不能以静态装饰替代动态效果。
- Windows 以外平台、账号与联网服务、云存档、复杂经济、任务惩罚、正式桌面宿主均未纳入当前建设范围。

## 材料与权威边界

- 用户提供的创意材料：《桌面微缩农场项目：可行性与初期验证摘要》。历史外部路径为 E:/下载/创意方向_desktop_mini_farm_research_summary.md，2026-09-17交付复核时该路径不可用；本文件保留已确认基线，领域规则保留必要约束，不依赖该文件才能工作。
- 补充资料包位于 `docs/ref/wo_you_yi_pian_tian_starter_pack/`，包含 `wo_you_yi_pian_tian_game_design.md` 与 8 张概念图；已按用户要求将整个 `docs/ref/` 排除出 Git。它是本地可选参考，不是构建输入，克隆项目不要求存在该目录。
- 原概念图 `05_game_view_diorama.png` 的斜向全景与微缩空间层次是用户再次指出偏好的参考；图板辅助建筑、色彩和氛围。图片中的文字、角色、风车、村民系统和数量不直接成为需求或历史依据。
- 当前玩法、图像与资料统一进入 `docs/design-baseline/`，旧[制作草案](../docs/production-brief.md)标为历史。最初开发侧按整田操作、六田两作物、三个阶段与三装饰完成首版；此整田决定现由用户明确要求的每田4×4逐格混种取代。原固定整场12格或资料包36格／8×8建议仍不沿用，也不并行保留旧整田入口。当前工作记录位于 `docs/task/格子农田与画面重构/`，历史首版计划与氛围交接不可反向覆盖新需求。图片与公开来源通过Git LFS管理，不是实际游戏截图；开发侧检查不等于用户主观签收。
- 材料中的“建议让 Codex 完成”、技术验证顺序、MVP“必做”清单和仓库示例均是待评估建议，不构成执行授权，也不是已经核验的外部技术事实。提炼后的长期约定保留在受版本管理的规则与文档中，不直接覆盖本项目 `AGENTS.md`。
- 用户明确本项目兼具商业平台推广目标，在质量可控的前提下合理增加 Tripo 的实际使用。可生成整株、阶段或叶片部件，Blender 做整理与组装，Godot 做运行呈现；资产生成与合理处理已获无新总额度上限授权，按明确质量收益使用并记录成本，不盲目重复；每条对外内容至少明确展示一次 Tripo 名称及具体资产贡献，并区分对话协作、图片生成和后期处理。过程报告与副本统一放中文 `制作留档/`，按用户要求整目录忽略 Git；正式风格与可复用规则仍入库。此约定不授权自动对外发布。

- 当前约定以用户明确决定、本文件及各领域规则为入口。引擎版本以根 `.godot-version`、工程设置以 `Game/project.godot`、导出设置以 `Game/export_presets.cfg` 为准；运行入口见 README。项目技能来源、固定提交及适用限制见 [资料与工具](../docs/godot-resources.md)。

## 后续决策时机

- 工程不依赖额外 Godot MCP 或通用游戏框架。原生命令行与文本资源用于当前开发；有明确缺口再评估扩展。引擎和导出模板一起升级并验证，场景原型可运行不代表完整玩法或最终美术通过。
- 用户已授权自主执行，无需逐项询问；2026-09-17最新指令禁止后续使用子代理，取代早期并行派发要求；历史1.1确定首版闭环，最新逐格与画面要求优先，执行中按真实证据更新状态。种植、生长、存档和阶段模型进入当前建设范围，交易经济仍不纳入。田地尺寸、作物种类和数量、施肥、建造、日照影响、攀爬、季节与天气均不得由图像或旧资料清单自动升级为需求；有明确采用指令时直接推进，不因草案标签重复确认。
- 实现生长与存档时按 [模拟规则](farm-simulation.md) 明确时间、离线结算与恢复语义；具体数值随获准玩法确定。
- 桌面实验仅在后续任务涵盖时开展，不是开始窗口版游戏的强制前置门槛。独立宿主、IPC 及其语言待进入该阶段再选型，不预建进程和通信框架。
- 上述待定项不阻塞当前规则适配；涉及对应实现时解决，不提前扩大本次任务。

## 资料包中的范围差异

- 新资料提出约 36 格、6 种作物、5 个生长阶段、12 个装饰和猫鸡两类动物；技术验证部分又提出 8×8 网格。它们都是候选，不能同时充当默认验收标准，也不为容纳差异预建多尺寸兼容系统。
- 季节在系统介绍中建议 14～21 天一季，在 MVP 中又列为暂缓。当前不纳入首个可玩切片；后续采用时先确定范围，并与现实昼夜的时钟语义分开。
- 新材料建议游戏主体稳定后再验证桌面宿主；继续沿用已确认的宿主后置原则，不让早期材料中的桌面实验成为强制前置条件。

## 当前验证与交付

- 规则任务执行 [testing.md](testing.md) 的 L0 验证；工程配置按需核验导入、启动与 Windows 导出。引擎切换不开展对比样片或付费生成测试，正式玩法、资产链路与性能仍须随对应任务验证。
- CLI 命令与验证证据入口见开发准备文档；无项目长期开发服务。执行命令时明确目标工程，不能用网页服务替代 Godot 验证，也不能误操作用户其他编辑器。
- 实际启动与构建入口见开发准备文档；新增玩法或正式测试时同步记录对应入口。测试存档与玩家存档隔离，运行外部工具前核实其数据、费用和权限边界。
- Git 提交与同步遵循 [git-workflow.md](git-workflow.md)，保护已有未跟踪文件；没有上游时不擅自选择推送目标。参考目录的忽略不代表其中内容被删除或已备份。

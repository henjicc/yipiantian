# AGENTS.md

> 项目级 AI 协作入口。默认使用中文；用户另有要求时遵循用户要求。

## 首次进入与初始化状态

每次进入项目或上下文丢失后，先读取 [`rules/project-context.md`](rules/project-context.md) 顶部的 `template_state`，它是初始化状态的唯一来源。

- `pending`：必须按 [`rules/project-initialization.md`](rules/project-initialization.md) 完成项目调查和规则适配，再开始业务实现；不能只提出初始化建议就结束。
- `ready`：直接执行当前任务，不重复初始化。后续事实变化按规则维护流程更新，不重置状态。
- 上下文文件或状态缺失、值不合法或状态与内容明显矛盾：按初始化规则调查和恢复，不能覆盖已有项目约定或默认初始化完成。
- 用户当前任务是维护这套初始化模板时，保留 `pending` 和可供复制的栏目，不将模板填成业务项目。只分析、审查或明确禁止写入的请求始终只读，初始化和规则维护不能越过该边界。

## 沟通、权限与持续执行

- 用户当前明确要求优先；项目内约定按更深目录的 `AGENTS.md`、本文件、对应细则、既有实现的顺序适用；不覆盖运行环境的上级指令与权限限制。
- 先自行调查代码、配置和可核验事实。低风险细节采用合理默认值；只有缺失决定会实质改变范围、安全、数据或不可逆结果时，才询问最少必要问题，并继续不依赖该决定的工作。
- 用户明确要求的功能是需求依据。已授权范围内的调查、实现、修复和相关验证应持续完成，不在首版实现后无故停下，也不重复索取已有授权。
- 不扩大产品范围，不顺手重构无关模块；不额外生成总结文档、示例项目或临时脚本文件，除非任务需要或用户要求。
- 若规则导致暂停、请求确认或无法完成，指出具体文件与条款，说明缺少的决定或权限，区分明确要求与自己的推断。
- 不删除、覆盖、stash 或误提交用户已有改动；不泄漏敏感信息，不伪造成功。不擅自执行强制推送、历史改写、真实数据删除、生产变更、发布、部署、付费或对外发消息。
- 本任务的普通提交和推送已配置的当前上游按 Git 规则自主执行；其他外部副作用确认现有授权是否覆盖对象、环境和范围，缺失时才请求授权。

## 控制复杂度

- 当前为早期开发阶段，不承诺旧存档兼容，不为旧数据新增迁移、兼容分支或对应测试；当前功能需要时可调整格式。改动验证只覆盖实际影响，不因完整历史测试集存在就全部运行。交付开发预览按测试规则自动识别并重启本项目开发游戏，静音、第二屏全屏；不需要逐次询问。

- 采用满足当前需求和质量要求的最小完整方案。新增抽象、配置、依赖、兜底或测试设施，应能指出其服务的当前需求、真实边界或可核验风险；“以后可能用到”“更通用”“更保险”本身不是依据。能安全决定时直接选择较简单方案，不增加审批或论证文档。
- 实现应行为正确、职责清晰、命名准确，正常流程与实际失败路径都完整；必要的函数拆分、模块边界和抽象应主动落实。以整体可读性、修改范围和维护成本衡量简单，不追求最少行数、文件数或测试数；不能省略必要的安全校验、失败处理和回归验证，也不能为少改几行保留根因或堆叠补丁。

## 按需读取

按当前要改变的行为或边界选择规则，在对应操作前读取；不要因提到某个技术名或执行普通文件读取就加载全部细则。进入新领域时补读；已经读过且未变化的内容不重复加载。

| 当前任务 | 读取规则 |
|---|---|
| 初始化、状态恢复或整体调整模板 | [`project-initialization.md`](rules/project-initialization.md) |
| 查找项目事实、权威入口、环境限制 | [`project-context.md`](rules/project-context.md) |
| 改变窗口、焦点、生命周期或 Windows 桌面能力 | [`platform.md`](rules/platform.md) |
| 核验外部契约、版本行为、方案选型或不确定事实 | [`research.md`](rules/research.md)；常用入口见 [Godot 资料及工具](docs/godot-resources.md) |
| 引入抽象、通用封装或依赖，改变模块职责、依赖方向、公共契约或状态归属 | [`architecture.md`](rules/architecture.md) |
| 新增功能、设置、兼容承诺、持久化状态、权限或常驻开销 | [`product-scope.md`](rules/product-scope.md) |
| 修改游戏界面、镜头、场景视觉、模型规范、文案或交互 | [`frontend-ui.md`](rules/frontend-ui.md) |
| 修改生长、日照玩法、离线结算、时间处理或农场存档 | [`farm-simulation.md`](rules/farm-simulation.md) |
| 实现或修改 I/O、后台任务的错误、恢复、日志或状态链路 | [`observability.md`](rules/observability.md) |
| 实现输入校验、鉴权、敏感数据处理或有外部/破坏性副作用的操作 | [`security.md`](rules/security.md) |
| 开始仓库改动、处理并行工作、提交或同步 | [`git-workflow.md`](rules/git-workflow.md) |
| 决定验证范围、验证改动、录制／渲染视频素材或交付运行结果 | [`testing.md`](rules/testing.md) |
| 项目事实失效、稳定约束变化、发现可复用开发经验、规则冲突或维护规则 | [`rule-maintenance.md`](rules/rule-maintenance.md) |

项目内技能位于 `.agents/skills/`。生成／替换三维资产、拆件动画、减面／LOD 破面排查，或检索／补拍这些资产的制作对比素材时，使用项目级 [farm-3d-asset-pipeline](.agents/skills/farm-3d-asset-pipeline/SKILL.md)；它路由到现有资产规范和素材记录。Godot 开发和 Blender 资产制作时按需使用对应技能。技能示例与社区资料不能替代官方版本核验，不覆盖当前项目规则或扩大任务范围；采用情况与限制见 [Godot 资料及工具](docs/godot-resources.md)。

链接是阅读入口，不要求递归读取所有被引用文件；实际触发对应领域时再读取。初始化整体裁剪时需检查全部规则的适用性。

## 完成与主动维护

- 交付以当前请求为边界：检查本次新增的结构、分支和测试是否有实际作用，移除无依据的新增内容；实现、必要验证、约定的开发服务处理和 Git 同步分别取得证据后交付。不继续添加假想场景或无关优化；没有新变化、失败或风险证据时不重复验证。
- 开发中发现已验证、后续可复用的操作经验或排错方法时，按 [`rule-maintenance.md`](rules/rule-maintenance.md) 及时更新对应文档，不等用户提醒；每个完整改动交付前再检查项目事实、规则、操作入口和长期约束是否需要同步。无新增知识或事实变化时不为形式而修改。
- 最终回复聚焦完成内容、关键取舍、实际验证、提交与同步结果，以及必要的未验证项；规则有同步变化时简述。开发服务状态按测试规则报告。
